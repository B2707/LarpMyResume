# frozen_string_literal: true
require 'rake'

begin
  require 'rspec/core/rake_task'
  RSpec::Core::RakeTask.new(:spec)
  task default: :spec
rescue LoadError
  desc 'spec (RSpec not available)'
  task :spec do
    warn 'RSpec not available — run: bundle install'
  end
end
